// 自动导入了lang包 不需要导入包了
// java.lang.Object org.omg.CORBA.Object;





Clone:
方法clone是为了复制一份一模一样的对象,当在进行引用传递的时候,就不需要考虑会不会将原来的应用里面的对象改变.
因为这时候的两个对象时相互独立的,互不影响.
为一个类设计clone的思路:
在该类中重写方法clone,将访问权限打开,方便其他类克隆改对象.
另外两种高级的克隆:影子clone"和"深度clone