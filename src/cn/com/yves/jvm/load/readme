讨论  JVM的加载顺序.

结论:
static 定义的部分不需要实例化就已经加载,而普通么有static修饰的部分是要等到实例化化该类的时候才加载.


具体的顺序,有static修饰的部分:
static  block(静态块)
static (静态成员变量)  静态块和静态成员变量哪个写在前面,哪个先加载执行.
相比下static method (静态成员方法),则是等着外部的调用,跟书写的顺序无关,因为在方法的内部如果用的静态属性会在执行该方法之前就已经初始化了.
所以只需要注意静态块和静态成员变量的顺序就可以了.

非static修饰的部分,也就是普通的成员,普通的代码块,普通的方法.都是是在实例化该时才初始化的,同样,在非static修饰的部分,普通的代码块和普通的成员变量是有顺序的,跟书写的顺序相关.
方法是没有顺序的,都是等着被调用时触发.
